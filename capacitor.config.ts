import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.wonderethiopia.app',
  appName: 'wonderethiopia',
  webDir: 'public'
};

export default config;
